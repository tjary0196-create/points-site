{
  "name": "digital-syria-store",
  "version": "1.0.0",
  "description": "متجر Digital Syria Store — موقع ثابت (HTML/CSS/JS) على خطة Firebase Spark المجانية بالكامل، بدون Cloud Functions وبدون أي باك-إند Node منفصل. كل الحماية منفّذة بقواعد Firestore (firestore.rules) وحدها.",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --only hosting,firestore:rules",
    "deploy:hosting": "firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules"
  },
  "license": "UNLICENSED"
}
